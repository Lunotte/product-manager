/**
 * Supprime les espaces de début et de fin d'une chaîne.
 * @param data Chaîne à nettoyer.
 * @returns Chaîne sans espaces en début/fin.
 */
export const cleanStartAndEndString = (data: string): string => data.trim();